---
phase: 07-confidence-triggers
plan: 04
type: execute
wave: 2
depends_on: [07-02, 07-03]
files_modified: []
autonomous: false
requirements: [INTV-03]

must_haves:
  truths:
    - "Agent card shows cyan pulsing dot while AskUser question is pending"
    - "Cyan panel with question text and answer buttons appears below card content"
    - "Clicking 'Let the team decide' closes the panel and the agent resumes"
    - "A second AskUser call from the same agent appends to the open card rather than opening a new one"
    - "Full test suite is green before this plan is marked done"
  artifacts:
    - path: "test/loomkin/teams/agent_confidence_test.exs"
      provides: "All tests passing (not skipped)"
    - path: "test/loomkin_web/live/workspace_live_ask_user_test.exs"
      provides: "All tests passing (not skipped)"
  key_links:
    - from: "Visual: cyan dot on agent card"
      to: "bg-cyan-500 animate-pulse CSS class in agent_card_component.ex"
      via: "status_dot_class(:ask_user_pending)"
    - from: "Visual: panel renders below card content"
      to: "border-t border-cyan-500/30 bg-cyan-950/20 div"
      via: ":if={@card.status == :ask_user_pending && @card[:pending_questions] != []}"
---

<objective>
Visual verification of the complete confidence trigger flow and final test suite gate before the phase is marked complete.

Purpose: Confirms the full user-facing experience works as described in INTV-03 success criteria — including the cyan visual identity, batched card behavior, and "Let the team decide" flow.
Output: Human sign-off that the feature works end-to-end. Full suite green.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-confidence-triggers/07-02-SUMMARY.md
@.planning/phases/07-confidence-triggers/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final test suite gate</name>
  <files></files>
  <action>
Run the full test suite and fix any remaining failures before the visual verification checkpoint:

1. Run `mix test` — fix any failures
2. Run `mix format` — fix any formatting issues
3. Run `mix precommit` — validate everything

If any test in `test/loomkin/teams/agent_confidence_test.exs` or `test/loomkin_web/live/workspace_live_ask_user_test.exs` is still marked `@tag :skip`, evaluate whether it needs implementation or should remain as a known gap with a code comment explaining why.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test 2>&1 | tail -10</automated>
  </verify>
  <done>mix test exits 0 with 0 failures. mix precommit passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of confidence trigger UI</name>
  <action>Human verifies the end-to-end confidence trigger flow in the live application.</action>
  <verify>
    <automated>MISSING — visual-only verification required for CSS animation and interactive flow</automated>
  </verify>
  <done>Human confirms: cyan dot visible, batched panel renders, "Let the team decide" resolves the card, rate-limit drop prevents second card within cooldown.</done>
  <what-built>
    Phase 7 confidence triggers: agent rate-limited AskUser batching, cyan card panel, "Let the team decide" collective fallback. Agent GenServer gains last_asked_at and pending_ask_user state fields. AgentCardComponent gains cyan panel below card content with sequential question list.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `mix phx.server` — navigate to http://loom.test:4200
    2. Open a team workspace with at least one active agent
    3. Trigger an AskUser call from an agent (or via test fixture) — verify:
       - Agent card shows cyan pulsing dot (not amber, not violet)
       - Status label reads "Waiting for you"
       - Cyan panel appears below the main card content area (not as an overlay)
       - The question text and answer buttons are visible
    4. Answer the question using one of the per-question buttons — verify card closes and agent resumes
    5. Trigger a second AskUser from the same agent within 5 minutes while the first card is open — verify the second question APPENDS to the existing panel (not a new card)
    6. Click "Let the team decide" on a card with pending questions — verify the panel closes and the agent receives a collective answer and continues
    7. Trigger an AskUser, answer it, then trigger another within 5 minutes (card is closed, cooldown active) — verify NO new card appears (drop behavior)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms: cyan dot, batched panel, "Let the team decide" flow, rate-limit drop all work as described in INTV-03 success criteria.
</verification>

<success_criteria>
- Full test suite green (mix test exits 0)
- Human confirms cyan visual identity correct
- Human confirms batching works (second question appends)
- Human confirms "Let the team decide" resolves card
- Human confirms rate-limit drop behavior (no second card within cooldown)
</success_criteria>

<output>
After completion, create `.planning/phases/07-confidence-triggers/07-04-SUMMARY.md`
</output>
