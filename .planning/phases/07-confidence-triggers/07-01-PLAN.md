---
phase: 07-confidence-triggers
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/loomkin/teams/agent_confidence_test.exs
  - test/loomkin_web/live/workspace_live_ask_user_test.exs
autonomous: true
requirements: [INTV-03]

must_haves:
  truths:
    - "Both test stub files exist and compile"
    - "Each stub file has at minimum one failing @tag :skip test per behavior under test"
    - "Running mix test on either stub file exits non-zero with compile errors only if code is missing, not if tests are skipped"
  artifacts:
    - path: "test/loomkin/teams/agent_confidence_test.exs"
      provides: "Failing stubs for rate limit guard, batch append, drop behavior, and cooldown semantics"
    - path: "test/loomkin_web/live/workspace_live_ask_user_test.exs"
      provides: "Failing stubs for let_team_decide event, batched card update, cyan accent rendering"
  key_links:
    - from: "test/loomkin/teams/agent_confidence_test.exs"
      to: "Loomkin.Teams.Agent"
      via: "module alias"
    - from: "test/loomkin_web/live/workspace_live_ask_user_test.exs"
      to: "LoomkinWeb.WorkspaceLive"
      via: "Phoenix.LiveViewTest"
---

<objective>
Create failing test stub files for Phase 7 (Wave 0 requirement — Nyquist compliance). These stubs define the behavior contracts before implementation begins.

Purpose: Ensures automated verification commands are available for Wave 1 plans from the first task. Stubs compile but tests are marked @tag :skip so the suite stays green.
Output: Two test files with skipped test cases covering every INTV-03 behavior.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-confidence-triggers/07-CONTEXT.md
@.planning/phases/07-confidence-triggers/07-RESEARCH.md
@.planning/phases/07-confidence-triggers/07-VALIDATION.md

<interfaces>
<!-- Existing test model files for fixture patterns -->
<!-- From test/loomkin/tools/request_approval_test.exs and test/loomkin_web/live/workspace_live_approval_test.exs -->

Existing approval test structure (model for confidence tests):
```elixir
# test/loomkin_web/live/workspace_live_approval_test.exs
defmodule LoomkinWeb.WorkspaceLiveApprovalTest do
  use LoomkinWeb.ConnCase, async: true
  import Phoenix.LiveViewTest
  alias Loomkin.Teams.Agent
  # ...
end
```

Agent GenServer struct fields (current, before Phase 7):
```elixir
defstruct [
  :team_id, :session_id, :name, :role, :role_config,
  :status, :model, :project_path,
  tools: [], messages: [], task: nil, context: %{},
  cost_usd: 0.0, tokens_used: 0, failure_count: 0,
  permission_mode: :auto, pending_permission: nil,
  loop_task: nil, pending_updates: [], priority_queue: [],
  pause_requested: false, pause_queued: false,
  paused_state: nil, subscription_ids: []
]
```

WorkspaceLive socket assigns relevant to AskUser (current):
```elixir
pending_questions: []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent confidence test stubs</name>
  <files>test/loomkin/teams/agent_confidence_test.exs</files>
  <action>
Create `test/loomkin/teams/agent_confidence_test.exs` as a stub file that compiles and defines skipped tests for every rate-limit behavior.

Model the module structure on `test/loomkin/tools/request_approval_test.exs`.

Include these test cases (all `@tag :skip`):

1. `"rate limit: first AskUser call is allowed through"` — assert GenServer.call(pid, {:check_ask_user_rate_limit, %{}}) returns :allow when no pending card exists
2. `"rate limit: second AskUser while card open appends to existing card"` — assert returns {:batch, card_id} when pending_ask_user is non-nil
3. `"rate limit: second call within cooldown window when card is closed is dropped"` — assert returns :drop when last_asked_at is within 5 minutes
4. `"rate limit: call after cooldown expires is allowed"` — assert returns :allow when last_asked_at > 5 minutes ago
5. `"rate limit: dropped call does not create a new card"` — assert state unchanged after :drop
6. `"batch: answers route to correct question by question_id"` — assert {:ask_user_answer, question_id, answer} is sent to the correct blocked process
7. `"cooldown: starts from when last question in batch is answered, not from card open"` — assert last_asked_at is only set on answer receipt

Use `@moduletag :skip` at the top so all tests skip. The file must compile. Use `assert false, "not implemented"` as the test body placeholder.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin/teams/agent_confidence_test.exs --no-start 2>&1 | grep -E "0 tests|skipped|Finished"</automated>
  </verify>
  <done>File exists, compiles without errors, all tests report as skipped (0 failures)</done>
</task>

<task type="auto">
  <name>Task 2: Create workspace live ask user test stubs</name>
  <files>test/loomkin_web/live/workspace_live_ask_user_test.exs</files>
  <action>
Create `test/loomkin_web/live/workspace_live_ask_user_test.exs` as a stub file that compiles and defines skipped tests for WorkspaceLive and AgentCardComponent AskUser behaviors.

Model the module structure on `test/loomkin_web/live/workspace_live_approval_test.exs`.

Include these test cases (all `@tag :skip`):

1. `"ask_user card: renders with cyan accent when agent has pending_questions"` — assert card wrapper has cyan border class when status is :ask_user_pending
2. `"ask_user card: shows sequential question list when multiple questions are batched"` — assert multiple question elements render in one card panel
3. `"ask_user card: each question has its own answer buttons with correct question-id"` — assert phx-value-question-id scoped per question
4. `"let_team_decide: triggers collective decision and removes card"` — assert handle_event("let_team_decide") calls handle_collective_decision and clears pending_questions for that agent
5. `"let_team_decide: resolves all pending questions in batch simultaneously"` — assert all blocked question tool tasks receive answers
6. `"status dot: shows bg-cyan-500 animate-pulse when status is :ask_user_pending"` — assert status dot class matches
7. `"status label: shows 'Waiting for you' when status is :ask_user_pending"` — assert label text

Use `@moduletag :skip` at the top. Use `assert false, "not implemented"` as test body placeholder. File must compile.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin_web/live/workspace_live_ask_user_test.exs --no-start 2>&1 | grep -E "0 tests|skipped|Finished"</automated>
  </verify>
  <done>File exists, compiles without errors, all tests report as skipped (0 failures)</done>
</task>

</tasks>

<verification>
Both test files compile and all tests are skipped (0 failures, 0 errors). The suite-wide `mix test` remains green.
</verification>

<success_criteria>
- `test/loomkin/teams/agent_confidence_test.exs` exists with 7 skipped test cases
- `test/loomkin_web/live/workspace_live_ask_user_test.exs` exists with 7 skipped test cases
- `mix test` exits 0 (all skips, no compile errors)
</success_criteria>

<output>
After completion, create `.planning/phases/07-confidence-triggers/07-01-SUMMARY.md`
</output>
