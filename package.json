{
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "lint:js": "oxlint apps/ packages/",
    "fmt:js": "oxfmt --write apps/ packages/",
    "fmt:js:check": "oxfmt --check apps/ packages/"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0"
  }
}
